# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python / backend
backend/.python-rt/
backend/.venv/
backend/venv/
__pycache__/
*.py[cod]
.python-version
.pytest_cache/
backend/.env

# Downloaded media
downloads/
backend/downloads/

# Env files
.env

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Tauri / Rust
src-tauri/target/
src-tauri/gen/
src-tauri/binaries/

# PyInstaller
backend/dist/
backend/build/
*.spec

# Bundled ffmpeg/ffprobe (fetched via scripts/fetch_ffmpeg.py)
backend/vendor/

# Generated OpenAPI types (reference only; the enforced guard is the contract test)
src/types/api.generated.ts

# Browser-extension build output + packaged release zips (redundant with the bare
# `dist` above, kept explicit for clarity)
extension/dist/
extension/web-ext-artifacts/
# AMO / Web Store API credentials (never commit) — template is .env.example
extension/.env
