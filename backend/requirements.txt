fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
# [default] pulls requests/urllib3/brotli/etc (better request handlers);
# [curl-cffi] enables impersonation (browser TLS fingerprint) to get past
# Cloudflare/anti-bot 403s. curl_cffi needs a Python with matching wheels —
# the backend runs on Python 3.13 (see docs/releasing.md), not 3.14.
yt-dlp[default,curl-cffi]>=2024.0
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
# Audio auto-tagging looks up metadata + cover art on the Apple Music (iTunes)
# catalogue over plain HTTPS (stdlib urllib) and writes the tags with mutagen,
# which already ships with yt-dlp[default]. No extra dependencies needed.
